using System;
using System.Windows.Forms;

namespace Minotti.Views
{
    // Migración de PowerBuilder: w_reconectar.srw (window) desde w_response
    // Mantiene el nombre del tipo: w_reconectar
    public partial class w_reconectar : Form
    {
        public w_reconectar()
        {
            InitializeComponent();
        }

        // Si en PB había eventos (open/close/click), los incorporamos cuando me pases esa lógica.
    }
}
